# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'cyrano_renewal' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod ‘Alamofire'
    pod 'SwiftyJSON'
    pod 'Kingfisher'
    pod 'ObjectMapper'
    pod 'AlamofireObjectMapper'

  # Pods for cyrano_renewal

  target 'cyrano_renewalTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cyrano_renewalUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
